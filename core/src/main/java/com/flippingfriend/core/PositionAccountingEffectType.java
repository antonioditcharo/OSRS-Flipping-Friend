package com.flippingfriend.core;

/** Economic meaning of one accepted or rejected lifecycle observation. */
public enum PositionAccountingEffectType
{
    NONE,
    ACQUIRE,
    DISPOSE,
    RECONCILE
}
